// Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with their data type.

// strings
let variableString = "Manish";
console.log(`
    value = ${variableString} ,
    data type = ${typeof(variableString)}
`);

//boolean
let variableBoolean = true;
console.log(`
    value = ${variableBoolean},
    data type = ${typeof(variableBoolean)}
`);

//undefined
console.log(`
    value = Manish,
    data type = ${typeof(this.value)}
`);

//null 
console.log(`
    data type = ${typeof(null)}
`);